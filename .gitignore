# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Single package manager: Bun (bun.lock). Keep other managers' lockfiles out.
package-lock.json
bun.lockb
yarn.lock
pnpm-lock.yaml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local working assets (source media staged for the site, not for the repo)
Replace images/

# Claude Code local config (dev-server launch config, etc.)
.claude/
